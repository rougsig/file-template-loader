package ${PACKAGE_NAME}

import ${MODEL_CLASS_NAME}
import ${MODEL_IMPL_CLASS_NAME}
import toothpick.config.Module

class ${SIMPLE_NAME} : Module() {
  init {
    bind(${MODEL_SIMPLE_NAME}::class.java)
      .to(${MODEL_IMPL_SIMPLE_NAME}::class.java)
      .singletonInScope()
  }
}
